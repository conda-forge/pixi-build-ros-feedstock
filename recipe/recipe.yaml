# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: pixi-build-ros
  crate_name: pixi_build_ros
  version: "0.4.1"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://github.com/prefix-dev/pixi/archive/refs/tags/${{ name }}-v${{ 
    version }}.tar.gz
  sha256: aeaddd64f1b7caa8343d38b843bce9ae75815ee8116c8b3537db25f024d8c62e

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
    - if: osx and x86_64
      then:
          # use the default linker for osx-64 as we are hitting a bug with the conda-forge linker
          # https://github.com/rust-lang/rust/issues/140686
      - unset CARGO_TARGET_X86_64_APPLE_DARWIN_LINKER

    - if: unix
      then:
      - export OPENSSL_DIR="$PREFIX"
    - if: win
      then:
      - set CARGO_HOME=C:\.cargo
      - md %CARGO_HOME%
    - cargo auditable install --locked --no-track --bins --root ${{ PREFIX }} 
      --path crates/${{ crate_name }} --no-default-features --features 
      native-tls
    - cargo-bundle-licenses --format yaml --output ./THIRDPARTY.yml
  files:
  - bin/${{ name }}
  - bin/${{ name }}.exe

requirements:
  build:
  - ${{ compiler("rust") }}
  - ${{ stdlib("c") }}
  - cargo-bundle-licenses
  - cargo-auditable
  host:
  - pkg-config
  - if: unix
    then: openssl
  run:
  - pixi-build-api-version >=4,<5

tests:
- script: ${{ name }} --help
- package_contents:
    bin:
    - ${{ name }}


about:
  homepage: https://github.com/prefix-dev/pixi
  summary: ROS build backend for Pixi
  description: |
    This package provides a ROS build backend for Pixi, allowing the integration of ROS packages into the Pixi build system.
  license: BSD-3-Clause
  license_file:
  - LICENSE
  - THIRDPARTY.yml
  documentation: https://pixi.prefix.dev/latest/build/backends/pixi-build-ros/
  repository: https://github.com/prefix-dev/pixi

extra:
  recipe-maintainers:
  - Hofer-Julian
  - baszalmstra
  - ruben-arts
